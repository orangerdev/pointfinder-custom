(function( $ ) {
	'use strict';

	$(document).ready(function(){

	});

	$('.shop-item-images').owlCarousel({
		loop:true,
		margin:10,
		nav:false,
		autoplay:true,
		autoplayTimeout:2000,
		dots:false,
		items:1
	});

})( jQuery );
